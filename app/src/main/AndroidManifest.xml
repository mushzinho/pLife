<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mush.com.br.blife">

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <meta-data
            android:name="DATABASE"
            android:value="SUGAR_BLIFE_DB" />
        <meta-data
            android:name="VERSION"
            android:value="11" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />

        <activity android:name=".MainActivity">



        </activity>

        <activity
            android:name=".AdicionarDoadorActivity"
            android:label="@string/adicionar_doador_titulo"
            android:parentActivityName=".MainActivity">

        </activity>

        <activity android:name=".AdicionarDoacaoActivity"
            android:label="@string/adicionar_doacao_titulo">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>